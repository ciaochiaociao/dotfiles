# Hosts to sync dotfiles to (one per line)
# Lines starting with # are ignored, blank lines are skipped
# Use SSH aliases or user@host format
#
# Example:
# devbox1
# user@workstation2.corp
# personal-laptop
